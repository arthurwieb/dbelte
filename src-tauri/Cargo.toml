[package]
name = "dbelte"
version = "0.1.3"
description = "Lightweight desktop database manager for PostgreSQL, MySQL, SQL Server and SQLite"
authors = ["Arthur Machado Wiebusch"]
license = "MIT"
repository = "https://github.com/arthurwieb/dbelte"
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.11.3", features = [] }
tauri-plugin-log = "2"
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"
tauri-plugin-clipboard-manager = "2"
sqlx = { version = "0.8", default-features = false, features = [
  "runtime-tokio",
  "tls-rustls",
  "macros",
  "postgres",
  "sqlite",
  "mysql",
  "json",
  "chrono",
  "uuid",
  "rust_decimal",
] }
# sqlx dropped its MSSQL driver after 0.6, so SQL Server goes through tiberius.
# rustls to match the TLS stack sqlx already pulls in; tds73 for datetime2 and
# friends, which anything since SQL Server 2008 sends.
tiberius = { version = "0.12", default-features = false, features = [
  "rustls",
  "tds73",
  "chrono",
  "rust_decimal",
] }
deadpool-tiberius = { version = "0.1", default-features = false, features = [
  "rustls",
  "tds73",
  # named instances listen on a dynamic port, resolved by the SQL Browser service
  "sql-browser",
] }
keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }
uuid = { version = "1", features = ["v4"] }
chrono = "0.4"
tokio = { version = "1", features = ["sync", "macros"] }
rust_decimal = "1"
csv = "1"
